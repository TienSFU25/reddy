

// sample function
function increment(x) {
    x = x + 1;
}

// another one
x = 5
function increment() {
    x = x + 1;
}

// functional way
function increment(x) {
    return x + 1;
}

originalString = "banA^&^&^&NA"

function removeCrap(string) {
    return another_string;
}

function lowercase(string) {
    return another_string;
}

yourFinalString = lowercase(removeCrap(originalString));

"banana"


// functional means no shared state
// no mutation

// initial state (t = 0)
_
_
_

// t = 5
_ _
_ _
_
_ _ _ _